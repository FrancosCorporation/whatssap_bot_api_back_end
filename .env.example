# Copie este arquivo para .env e preencha os valores locais.
# O arquivo .env NAO deve ser versionado.

# Conexao com o MongoDB
MONGO_URI=
NOME_DB=

# API de IA (ex.: Ollama em /api/generate)
URL_API=

# Autenticacao JWT
SALT_ROUNDS=
JWT_SECRET=
JWT_EXPIRES_IN=

# Ambiente / servidor
NODE_ENV=
PORT=
