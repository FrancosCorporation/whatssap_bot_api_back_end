version: '3.8'

services:
  ollama-backend:
    build: .
    container_name: ollama-backend
    restart: always
    ports:
      - "11434:11434"
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
    environment:
      - OLLAMA_HOST=0.0.0.0

